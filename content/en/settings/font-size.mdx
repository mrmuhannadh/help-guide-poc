import { FigureProvider, Figure, FigureRef } from '@/components/figure';

# Font Size

<FigureProvider>

Functionality can be used either to Increase or decrease the font size of the system.

**STEPS**

1. Click on Settings → Application Settings to view the **Settings** window, the following window display to the user.
2. In the list options (**Smaller**, **Small**, **Medium**, **Large**, **Larger**), select the font size you prefer

<Figure
  baseName='font-size/font-size'
  alt='Font Size'
  description='Font Size'
/>

</FigureProvider>
