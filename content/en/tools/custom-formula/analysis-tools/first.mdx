import { FigureProvider, Figure } from '@/components/figure';

# First

SYNTAX First(DATAARRAY)

Get first element in DataArray.

Eg:

```jsx
First('close')

a1 = MVSimple(100,close,MA);

out = First(a1);
```
<FigureProvider>
<Figure
  baseName='tools/analysis-tools/first'
  alt='First'
  description='First'
/>
</FigureProvider>