import { FigureProvider, Figure } from '@/components/figure';

# Fill Region

SYNTAX FillRegion(condition,upper,lower)

Fills background between two plots.

Eg:

```jsx
FillRegion(lineA < lineB, lineA, lineB)
```
<FigureProvider>
<Figure
  baseName='tools/analysis-tools/fill-region'
  alt='Fill Region'
  description='Fill Region'
/>
</FigureProvider>