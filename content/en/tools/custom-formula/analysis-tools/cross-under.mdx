import { FigureProvider, Figure } from '@/components/figure';

# Cross Under

SYNTAX crossesUnder(source1, source2)

The source1-series is defined as having crossed under source2-series if, on the current bar, the value of source1 is less than the value of source2, and on the previous bar, the value of source1 was greater than or equal to the value of source2.

Eg:

```jsx
a1 = MVSimple(100,close,MA);

a2 = MVSimple(10,close,MA);

out = crossesUnder(a1, a2);
```
<FigureProvider>
<Figure
  baseName='tools/analysis-tools/cross-under'
  alt='Cross Under'
  description='CrossUnder'
/>
</FigureProvider>