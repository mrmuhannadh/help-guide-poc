import { FigureProvider, Figure } from '@/components/figure';

# ABS

<FigureProvider>

Calculates the absolute value of the given field.

Eg:
```jsx
ABS(Close);

A1 = Open + Close;

ABS(A1);
```
</FigureProvider>