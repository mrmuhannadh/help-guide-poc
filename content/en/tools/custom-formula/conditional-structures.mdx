import { FigureProvider, Figure } from '@/components/figure';

# Conditional Structures

‘IF’ structure syntax:

```jsx
returnVal = 0

if (Open > Close) {
    returnVal = Open
} else {
    returnVal = Close
}
```
<FigureProvider>

<Figure
  baseName='tools/conditional-structures/if-function'
  alt='IF Function'
  description='Conditional Structure - If Open > Close'
/>

</FigureProvider>
